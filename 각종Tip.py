# VS Code나 터미널에서 실행할 때 현재 작업 디렉토리(Working Directory)가 소스 파일이 있는 곳과 달라서 발생하는 전형적인 경로 문제입니다.

# 매번 cd 명령어를 치기 번거로우시다면, 파이썬 코드 상단에 실행 파일의 위치로 작업 디렉토리를 자동 변경하는 코드를 추가하는 것이 가장 깔끔합니다.

# 1. 파이썬 소스 코드에 자동 경로 변경 추가 (추천)
# 코드 맨 윗부분(import 직후)에 아래 내용을 넣으면, 어느 폴더에서 실행하더라도 항상 소스 파일이 있는 폴더를 기준으로 동작하게 됩니다.


import os
import sys

# 현재 실행 중인 파일(one_image.py)이 있는 디렉토리로 작업 경로 변경
os.chdir(os.path.dirname(os.path.abspath(__file__)))

# 확인용 (필요 없으면 삭제)
print(f"현재 작업 디렉토리: {os.getcwd()}")

